export const FIXED_DEPOSIT = 250;
export const DEFAULT_PAYMENT_TYPE = 'deposit';
export const ALLOW_WORKSHOP_DROPOFF = true;
export const BOOKING_LEAD_HOURS = 36;
// Minimum order value BEFORE any coupon discount — a coupon can zero out
// what's actually charged, but it never waives the minimum order size.
export const MIN_SUBTOTAL = 900;
